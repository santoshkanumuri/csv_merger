[project]
name = "merger"
version = "0.1.0"
description = "CSV and Excel file merger with Streamlit UI"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.28.0",
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
]

[project.scripts]
merger = "app:main"
